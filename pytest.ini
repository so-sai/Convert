[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Fast unit tests (no I/O)
    integration: Module interaction tests
    security: Security enforcement tests (wipe, path traversal)

addopts = -v --strict-markers
pythonpath = . src