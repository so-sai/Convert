[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "docx-extractor"
version = "0.1.0"
requires-python = ">=3.14"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "docx_extractor"
